# 2026-03-20 데스크 홈 상단 빈 공간 제거 2차

## Plan

- 데스크톱 홈 첫 섹션에서 왼쪽 히어로 아래 빈 면처럼 보이는 인상을 줄인다.
- 오른쪽 안내 영역의 여러 카드 스택을 하나의 흐름으로 합쳐 상단 배치를 더 타이트하게 만든다.

## Do

- `src/App.tsx`
  - 오른쪽 안내 영역을 3개 카드 스택에서 1개 통합 카드로 바꿨다.
  - 카드 안에서 `단계 안내`, `핵심 정보`, `흐름 설명`을 섹션으로 나누고, 바깥 배치에서는 한 덩어리처럼 보이게 정리했다.
  - 세부 패딩과 블록 높이를 조금 더 압축해 상단 전체 높이를 줄였다.
  - 같은 그룹 카드에는 `auto-rows-fr`, `h-full`을 적용하고, 안내 문구를 줄여 카드 높이와 폭이 더 일정하게 보이도록 맞췄다.
  - 히어로 아래 설명 카드 행을 공통 전체 폭 행으로 분리해 큰 박스 경계가 어긋나지 않게 정리했다.

## Check

- 실행 명령
  - `npm run lint`
  - `npm run test`
  - `npm run build`
- 결과
  - `lint` 통과
  - `test` 통과
    - `8 files / 77 tests`
  - `build` 통과
- 관찰 사항
  - 기존 chunk size 경고와 plugin timing 경고는 그대로 유지됐다.

## Act

- 다음 루프에서는 첫 섹션에서 더 손볼 게 남는지, 아니면 하단 섹션 밀도 조정으로 넘어갈지 실제 화면 피드백을 보고 결정한다.
- 상단처럼 높이 차가 체감되는 영역은 카드 수를 먼저 줄이고, 그다음 패딩/타이포를 조정하는 순서가 가장 효율적이었다.
